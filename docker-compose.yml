services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      STORAGE_PATH: /storage
    volumes:
      # ホスト側のパスを変えるだけでデータ保存先を変更できる（再ビルド不要）
      # 例: /mnt/nas/image-viewer:/storage
      - ./storage:/storage
    restart: unless-stopped
